// Keep the established URL while routing every new project through the
// JSONB-backed role-budget flow. This prevents a second, divergent writer.
export { default } from "@/app/client/create-project/page";
